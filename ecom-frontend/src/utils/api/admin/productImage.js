import { API_BASE_URL } from "../base";

export const PRODUCT_IMAGE_API = {
  CREATE: `${API_BASE_URL}/admin/save-product-image`,
  UPDATE_DEFAULT_IMAGE: `${API_BASE_URL}/admin/change-product-default-image`,
};
